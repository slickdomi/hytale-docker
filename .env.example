# Hytale Server Docker Configuration

# Memory allocation (adjust based on server size and player count)
# Small server (4-10 players): 4G
# Medium server (20-30 players): 8G
# Large server (50+ players): 12G or more
HYTALE_MAX_MEMORY=4G

# Server port (default: 5520, must be UDP)
HYTALE_PORT=5520

# Bind address (0.0.0.0 for all interfaces)
HYTALE_BIND=0.0.0.0

# Enable AOT (Ahead-of-Time) caching for better performance
HYTALE_AOT_CACHE=true

# Auto-download server files on first run (true/false)
# If enabled, the container will automatically download HytaleServer.jar and Assets.zip
HYTALE_AUTO_DOWNLOAD=true

# Additional Java options (optional)
# Examples:
# - Use G1 garbage collector: -XX:+UseG1GC
# - Set GC pause time: -XX:MaxGCPauseMillis=50
# - Enable JMX monitoring: -Dcom.sun.management.jmxremote
JAVA_OPTS=

# View distance (recommended: 12 chunks / 384 blocks)
# Lower values improve performance
HYTALE_VIEW_DISTANCE=12
